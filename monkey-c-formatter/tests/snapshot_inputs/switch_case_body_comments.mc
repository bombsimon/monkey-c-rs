function f() {
    switch (x) {
    case 1: // inline comment
            // own-line comment

            System.println("x");
    case 2:
            // leading-only comment
            foo();
    }
}
