enum Color {
    COLOR_RED = 1,
    COLOR_GREEN = 2,
    COLOR_BLUE_DARK = 3,
}

enum {
    FIELD_SHORT = 1,
    FIELD_LONGER_NAME = 22,
    FIELD_LONGEST_NAME_HERE = 333,
}

enum State {
    STATE_A,
    STATE_B = 5,
    STATE_C = 6,
    STATE_D,
}

enum Stage {
    STAGE_A,
    STAGE_B = 5,
    STAGE_C,
}
