[package]
name = "monkey-c-formatter"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4", features = ["derive"] }
monkey-c-parser = { path = "../monkey-c-parser" }

[dev-dependencies]
insta = { version = "1", features = ["glob"] }
